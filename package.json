{
  "name": "46-grupe-dom-intro",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {

    "dev": "dead-server --host=localhost --port=4608",
    "render": "dead-server --host=localhost --port=4609",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Modestas Juska",
  "license": "MIT",
  "devDependencies": {
    "dead-server": "^1.0.9"
  }
}
